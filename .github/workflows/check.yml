name: dunewar_react
on: push
jobs:
    lint:
        runs: ubuntu-latest
        steps:
           -name: Install deps
            run: npm install
           -name: run
            run: npm run start